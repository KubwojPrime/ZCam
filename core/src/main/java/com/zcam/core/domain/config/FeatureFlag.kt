package com.zcam.core.domain.config

enum class FeatureFlag {
    MJPEG_STREAMING,
    LOOP_RECORDING,
    AUDIO_PUSH_TO_TALK,
    AUDIO_LIVE,
    AUDIO_PLAYBACK,
    TRUSTED_DEVICES,
    WATCHDOG_RECOVERY
}
